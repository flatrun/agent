name: test-proxy-manual
services:
  app:
    image: nginx:alpine
    container_name: test-proxy-manual
    volumes:
      - ./html:/usr/share/nginx/html:ro
    expose:
      - "80"
    networks:
      - web
    restart: unless-stopped

networks:
  web:
    external: true
