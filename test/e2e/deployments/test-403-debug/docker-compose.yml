name: test-403-debug
services:
  app:
    image: nginx:alpine
    container_name: test-403-debug
    volumes:
      - ./html:/usr/share/nginx/html:ro
    expose:
      - "80"
    networks:
      - web
    restart: unless-stopped

networks:
  web:
    external: true
