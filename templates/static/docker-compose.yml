name: ${NAME}
services:
  app:
    image: nginx:alpine
    container_name: ${NAME}
    volumes:
      - ./html:/usr/share/nginx/html:ro
    expose:
      - "80"
    networks:
      - web
    restart: unless-stopped

networks:
  web:
    external: true
